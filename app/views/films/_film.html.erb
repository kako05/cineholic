<%= link_to film_path(film), class: "film", data: {turbo: false} do %>
  <div class="left-show">
    <% if film.poster_image_url.present? && film.poster_image_url != "/global/images/jfdb_noimage.png" %>
      <%= image_tag(film.poster_image_url, class: "index-poster-image") %>
    <% else %>
      <%= image_tag(asset_path('noimage.png'), class: "index-poster-image noimage") %>
    <% end %>
  </div>
  <div class="right-show">
    <div class="title-show">
      <h2><%= film.title %></h2>
      <div class="release-date"><%= film.release_date %></div>
    </div>
    <div class="text-show">
      <p><%= film.description %></p>
    </div>
  </div>
<% end %>